{
  "name": "gus-3d",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "npm --prefix experiments/me ci --include=dev --no-audit --no-fund && npm --prefix experiments/me run build && node scripts/copy-me.mjs && next build",
    "start": "next start",
    "test": "node --test tests/*.test.mjs",
    "test:e2e": "playwright test",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@gusvega/ui": "1.0.0",
    "next": "^16.0.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "recharts": "3.8.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "prettier": "^3.9.6"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
